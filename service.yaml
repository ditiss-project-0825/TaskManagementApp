apiVersion: v1
kind: Service
metadata:
  name: task-app-service
spec:
  type: NodePort
  selector:
    app: task-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
